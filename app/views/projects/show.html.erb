<p id="notice"><%= notice %></p>

<h3><%= @project.title %></h3>

<p>
  <%= @project.description %>
</p>

  <br>

  <strong>Features:</strong>

<table class="table">
  <thead>
    <td>ID</td>
    <td>Title</td>
    <td>Description</td>
  </thead>
  <tbody>
<% @features.each do |feature| %>
    <tr>
      <td><%= link_to feature.id, project_feature_path(@project, feature) %></td>
      <td><%= feature.title %></td>
      <td><%= feature.descripion %></td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link_to 'New Feature!', new_project_feature_path(@project) %> |
<%= link_to 'Edit Project', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
